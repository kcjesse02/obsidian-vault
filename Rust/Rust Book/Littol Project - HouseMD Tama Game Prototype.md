## Creating a new rust project 
open an empty folder and just type
```bash
cargo init
```

### HouseChan - The character sturcture
He has a mood. This is the main attribute. 
it has a `happiness` and an `energy` field
`energy` will decrease with time and increase with time
`happiness` will probably start decreasing when energy gets below a certain threshold and also will increase and decrease based on feeding foods of certian prefs